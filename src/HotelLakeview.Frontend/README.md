# HotelLakeview Frontend

Vite + React + TypeScript + TanStack Query + date-fns frontend for the hotel reservation system.

## Scripts
- `npm run dev`
- `npm run build`
- `npm run preview`

## Environment
Set `VITE_API_BASE_URL` to point to the ASP.NET Core API when running locally.